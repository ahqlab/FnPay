<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.whyble.fn.pay">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".view.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.join.JoinActivity" />
        <activity android:name=".view.payment.PaymentActivity" />
        <activity android:name=".view.payment.list.PaymentListActivity" />
        <activity android:name=".view.exchange.ExchangeActivity" />
        <activity android:name=".view.send.SendActivity" />
        <activity android:name=".view.receive.ReceiveActivity" />
        <activity android:name=".view.editInfo.EdtInfoActivity" />
        <activity android:name=".test.TestActivity" />
        <activity android:name=".view.history.HistoryActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".view.send.pincheck.PinCheckActivity" />
        <activity android:name=".view.receive.request.RequestAnAmountActivity" />
        <activity android:name=".view.addressBook.AddressBookActivity" />
        <activity android:name=".view.change.pin.ChangePinActivity" />
        <activity android:name=".view.change.password.ChangePasswordActivity"></activity>
    </application>

</manifest>